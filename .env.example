# Environment Variables for SplitEasy PWA

# Backend API URL
# For local development:
VITE_API_URL=http://localhost:5000/api

# For production (update after backend deployment):
# VITE_API_URL=https://your-backend-app.onrender.com/api

# Note: If no backend is deployed, the app will use localStorage only
# Data will persist in the browser but not sync across devices
